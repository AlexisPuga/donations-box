const InjectionError = require('./injection-error');
const BuildError = require('./build-error');
const CopyError = require('./copy-error');

module.exports = {
	InjectionError,
	BuildError,
	CopyError
};
